# Closure module (API)

Project handover / closure checklist — logic mostly in route + Prisma today.

**Route:** `apps/api/src/routes/closure.ts`

## Planned layout

```
closure/
  index.ts                    add handover workbook export when client sheet arrives
```

**Web:** `apps/web/src/modules/closure/` — `ProjectClosurePage`.

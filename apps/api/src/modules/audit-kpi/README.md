# Audit & KPI module (API)

Site audit pack + Master KPI dashboard — import, CRUD, CSV/XLSX export.

**Route:** `apps/api/src/routes/auditKpi.ts`

```
audit-kpi/
  seedFromSheets.ts           SITE_AUDIT_Pack + MASTER_KPI_DASHBOARD import
  index.ts
```

**Reference workbooks:** `docs/SITE_AUDIT_Pack.xlsx`, `docs/MASTER_KPI_DASHBOARD.xlsx`  
**Web:** `apps/web/src/modules/audit-kpi/` — `AuditKpiPage` (hub `audit-kpi?tab=…`)

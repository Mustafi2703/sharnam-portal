# Reports module (API)

Report packs, quotation export, proposal DOCX — not DPR/WPR (see `dpr/`, `wpr/`).

**Route:** `apps/api/src/routes/reports.ts`

## Planned layout

```
reports/
  reportPacks.ts
  quotationExport.ts
  proposalTemplate.ts
  index.ts
```

**Web:** `apps/web/src/modules/reports/` — `ReportsPage`, `QuotationMakerPage`, `ReportExportButtons`.

# Custom sheets module (API)

Sheet maker — formula grid templates tied to cost master categories.

**Route:** `apps/api/src/routes/customSheets.ts`

## Planned layout

```
custom-sheets/
  index.ts                    imports master line helpers from cost module after migration
```

**Web:** `apps/web/src/modules/custom-sheets/` — `CustomSheetsPage`.

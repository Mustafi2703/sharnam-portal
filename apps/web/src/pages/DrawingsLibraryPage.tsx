/** Drawing files — SharePoint sheets under Design & Engineering (separate from Documents DMS). */
import DmsPage from "./DmsPage";

export default function DrawingsLibraryPage() {
  return <DmsPage mode="drawings" />;
}
